<form method="post" action="{{ path('app_diagnostic_delete_admin', {'id': diagnostic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
