<form method="post" action="{{ path('app_resultat_delete', {'id': resultat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat.id) }}">
    <button class="btn">Delete</button>
</form>
